<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cjp.smallappexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.WeiXinPopWindow"></activity>
        <activity android:name=".activity.DrawerAnnoucement"></activity>
        <activity android:name=".activity.QQMenu"></activity>
        <activity android:name=".activity.Satellite"></activity>
        <activity android:name=".activity.MessageActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>
        <activity android:name=".activity.CircleMenuActivity"></activity>
        <activity android:name=".activity.RainBowActivity"></activity>

    </application>

</manifest>